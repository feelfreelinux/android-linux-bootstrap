name: Build app
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '2.x'
      - name: Install NDK
        run: echo "y" | sudo ${ANDROID_HOME}/tools/bin/sdkmanager --install "ndk;21.3.6528147" --sdk_root=${ANDROID_SDK_ROOT}
      - name: Checkout submodules
        uses: textbook/git-checkout-submodule-action@master
      - name: Get android home
        run: echo ${ANDROID_HOME}
      - name: Build the bootstrap
        run: |
          mkdir -p ~/Android
          ln -s ${ANDROID_HOME} ~/Android/Sdk 
          sudo ./generate-bootstraps.sh
        working-directory: ./scripts
      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: Bootstrap
          path: ./scripts/build/bootstrap-aarch64.zip
